# flowcation-mcp

MCP-Server, der Coding-Agents (Claude Code, Junie, Cursor, Aider) Zugriff auf das Registry gibt:
`resolve`, `search`, `render`, `validate`, `lock`, `verify`. Nutzt dieselbe Logik wie `flowcation-cli`.

**Aktive Phase**: Phase 1 — siehe [Master-Plan](../.agent/plans/flowcation-plan.md). In Phase 0 noch leer.
