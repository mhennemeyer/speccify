"""CLI-Subcommands für Speccify (Phase 1a).

Hier wohnen `lock`, `add`, später `pull`, `verify`. Die Module sind dünne Adapter über
`speccify_core` und teilen sich einen `WorkspaceContext` aus `_workspace.py`.
"""
