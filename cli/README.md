# speccify-cli

Das `speccify`-CLI: `init`, `add`, `pull --target`, `lock`, `verify`, `publish`, `yank`, `search`, `lint`.

**Aktive Phase**: Phase 0 (`speccify lint`). Siehe [Master-Plan](../.agent/plans/speccify-plan.md), Phase 1 für die übrigen Befehle.
