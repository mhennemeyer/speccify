# flowcation-cli

Das `flowcation`-CLI: `init`, `add`, `pull --target`, `lock`, `verify`, `publish`, `yank`, `search`, `lint`.

**Aktive Phase**: Phase 0 (`flowcation lint`). Siehe [Master-Plan](../.agent/plans/flowcation-plan.md), Phase 1 für die übrigen Befehle.
