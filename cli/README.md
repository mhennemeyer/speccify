# speccify-cli

Das `speccify`-CLI: `init`, `add`, `pull --target`, `lock`, `verify`, `conformance`, `lint`.

**Aktive Phase**: Phase 0 (`speccify lint`). Siehe [Master-Plan](../.agent/plans/archive/speccify-plan.md), Phase 1 für die übrigen Befehle.
