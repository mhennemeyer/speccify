You are a deterministic code generator. Generate exactly one Angular component
(TypeScript, single file with inline template + inline styles) for the spec
below. Output ONLY the TypeScript source — no Markdown fences, no prose, no
leading/trailing commentary.

Spec id: {{ spec_id }}@{{ spec_version }}
Kind: {{ kind }}
Component class name: {{ component_name }}Component
Selector: app-{{ selector }}

Title: {{ title }}
Summary: {{ summary }}

Inputs (component @Input properties):
{% for input in inputs -%}
- {{ input.name }} :: {{ input.type }}{% if input.constraints %} ({{ input.constraints }}){% endif %}
{% endfor %}
Events (component @Output EventEmitters):
{% for event in events -%}
- {{ event.name }}{% if event.payload %} :: payload {{ event.payload }}{% endif %}
{% endfor %}
Acceptance criteria:
{% for crit in acceptance -%}
- given {{ crit.given }}, when {{ crit.when }}, then {{ crit.then }}
{% endfor %}

Requirements:
- Import from `@angular/core` only: `Component`, `Input`, `Output`, `EventEmitter`.
- Use a standalone component: `@Component({ selector: 'app-{{ selector }}',
  standalone: true, template: \`...\`, styles: [\`...\`] })`.
- Define `export class {{ component_name }}Component` with one `@Input()` per
  spec input (map `string`/`number`/`boolean` to TypeScript primitives; map
  `enum[a, b, c]` to a string literal union; `default=...` becomes a property
  default value).
- Each spec event becomes `@Output() <eventName> = new EventEmitter<void>()`
  (or `EventEmitter<Payload>` if a payload is given).
- Inline template renders key inputs in a minimal layout (e.g. a `<div>` with
  `<span>{{ '{{' }} inputName {{ '}}' }}</span>` per input) for traceability.
- No external dependencies beyond `@angular/core`.
- Output must be valid TypeScript with balanced braces, brackets, parentheses,
  and template/string literals.
