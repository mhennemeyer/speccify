# speccify-core

Geteilte Basis-Bibliothek: Spec-Loader, Schema-Validator, Resolver, Codegen-Pipeline.
Wird von `speccify-cli` und `speccify-mcp` gemeinsam genutzt — eine Implementierung, zwei Schnittstellen.

**Aktive Phase**: Phase 0 (`SpecLoader` + `SchemaValidator`). Siehe [Master-Plan](../.agent/plans/speccify-plan.md), Phase 1 für Resolver/Codegen.
