# flowcation-core

Geteilte Basis-Bibliothek: Spec-Loader, Schema-Validator, Resolver, Codegen-Pipeline.
Wird von `flowcation-cli` und `flowcation-mcp` gemeinsam genutzt — eine Implementierung, zwei Schnittstellen.

**Aktive Phase**: Phase 0 (`SpecLoader` + `SchemaValidator`). Siehe [Master-Plan](../.agent/plans/flowcation-plan.md), Phase 1 für Resolver/Codegen.
