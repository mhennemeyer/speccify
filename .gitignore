# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# uv — uv.lock IS checked in (do not ignore)

# Node / Web
node_modules/
.next/
.svelte-kit/
.turbo/
dist/
build/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Env
.env
.env.local

# Speccify smoke artifacts (regeneriert in CI; example-project hat keine permanente Output-Spur)
example-project/out/
example-project/speccify.lock

# Lokale Dev-DB (erzeugt von scripts/dev-up.sh; siehe docs/local-dev-e2e.md)
registry/db.sqlite3

# Rust (Cargo.lock IST eingecheckt — Binaries, kein Library-Crate)
target/

# Composer-Playwright-Smoke: Wegwerf-Bibliothek + Git-Fixture, beide vom Test
# bei jedem Lauf neu erzeugt (e2e/start-backend.sh).
apps/composer/e2e/.library-tmp/
apps/composer/e2e/.git-fixture/
# Hieß bis W1 `.registry-tmp`; die Umbenennung hier nachzuziehen wurde
# vergessen, seitdem wanderte die Kopie stillschweigend in jeden Commit. Der
# Eintrag bleibt, weil auf manchen Rechnern noch ein altes `.registry-tmp` mit
# Komponenten-Specs von vor dem Rückbau liegt — löschbar, erzeugt niemand mehr.
apps/composer/e2e/.registry-tmp/
apps/composer/test-results/
apps/composer/playwright-report/

# Agent-Sitzungsartefakte: rohe Chat-Transkripte mit absoluten Pfaden. Sie
# dokumentieren nichts, was `.agent/log.md` nicht besser sagt.
.agent/chats/
# Reste eines abgelösten Agent-Tools (durch `.agent/` ersetzt).
.junie/

# iCloud-Konfliktkopien. Das Repo liegt unter ~/Desktop, der bei aktivem
# „Schreibtisch & Dokumente" von iCloud synchronisiert wird; dabei entstehen
# Dubletten der Form `datei 2.ext`. Sie werden von pytest und ruff
# mitgesammelt und haben die lokale Testzahl still verfälscht.
* [0-9].*
* [0-9][0-9].*
* [0-9]
* [0-9][0-9]

# Materialisierte Skills — erzeugt aus skills/ bzw. dem Manifest.
# Wegwerfbar wie node_modules; die Quelle ist skills/.
# `.claude/skills` is a symlink to `.agent/skills` (speccify link) and is committed.
# The upstream cache is not: `speccify expand` derives everything from the lockfile.
.agent/speccify/cache/

# Private Schluessel/Zertifikate niemals ins Repo (Export liegt in ~/.speccify/)
*.p12

# Spec-Register: Worktree des Branch specs (Spec 028)
.agent/specs/

# Spec 031: team board generated from the specs branch at deploy time
apps/marketing/public/board/
