schema_version: 1
id: "@speccify/iap-trial-then-unlock"
version: 1.0.0
title: Seven-day free trial plus a one-time unlock (StoreKit 2, macOS/iOS)
summary: >
  Ship an app that is free to try for seven days and then costs one payment —
  from the App Store Connect product to a trial clock that survives
  reinstalls, new devices and a rewound system clock.
authors: [mhennemeyer@me.com]
license: MIT

applies_to:
  platforms: [macos, ios]
  requires: ["Xcode >= 16", "Apple Developer Program", "StoreKit 2 (iOS 15 / macOS 12+)"]
  keywords: [storekit, iap, trial, non-consumable, paywall, universal-purchase]

prerequisites:
  - An app record exists in App Store Connect
  - The paid applications agreement is active — without it products never load
  - You have decided what the app does *after* the trial (see the first step)

steps:
  - id: choose_model
    title: Decide the trial model — Apple does not give you one
    detail: |
      This is the fork that decides everything after it: **Apple's free trials
      are introductory offers, and introductory offers only exist for
      subscriptions.** A non-consumable "buy once" product cannot have one.

      So for "seven days free, then buy once" you have two options:

      1. **Subscription with an introductory offer** — Apple runs the clock,
         but the user rents rather than owns, and you inherit renewals,
         grace periods and billing retry.
      2. **Free app + non-consumable unlock + your own trial clock** — you own
         the clock, and the user owns the app. Everything below takes this
         route.

      Option 2 is the honest fit for a tool people expect to keep. Its price is
      exactly one hard problem: *where do you store the start date so it cannot
      be reset by deleting the app?* That is step `trial_storage`, and it is
      where the real work is.
    sources: [asc_iap_types, storekit_offers]
    verify: You can say in one sentence what the app does on day 8 without a purchase.

  - id: asc_product
    title: Create the non-consumable product in App Store Connect
    detail: |
      *Monetization → In-App Purchases → +* → **Non-Consumable**.

      The product ID is permanent — it can never be reused, even after you
      delete the product. Two things bite here:

      - It may only contain **letters, digits, underscores and dots**. If your
        bundle ID has a hyphen (`com.better-apps.thing`), the product ID cannot
        mirror it — pick something like `com.betterapps.thing.pro` and write
        down why the two differ.
      - Enable **Universal Purchase** if Mac and iOS share the bundle ID; one
        purchase then unlocks every platform.

      Fill in a localized display name and description for each language you
      ship — they appear in the system purchase sheet, not just in the store.
    sources: [asc_create_iap, universal_purchase]
    verify: The product shows up in App Store Connect with status "Ready to Submit".

  - id: storekit_config
    title: Add a StoreKit configuration file for local testing
    detail: |
      A `.storekit` file mirrors the product locally so the paywall works with
      no network and no App Store Connect round trip. Add it to the run scheme
      (*Edit Scheme → Run → Options → StoreKit Configuration*) — an
      unselected file does nothing, and the symptom looks like "products not
      found".

      The bundled asset is a minimal starting point: one non-consumable, one
      locale. Keep the product ID identical to App Store Connect.
    assets: [assets/Unlock.storekit]
    sources: [xcode_testing]
    verify: The paywall shows a localized price while the machine is offline.

  - id: store_service
    title: Wire up StoreKit 2 — entitlements, purchase, restore
    detail: |
      Four things matter, and three of them are ordering problems:

      1. **Subscribe to `Transaction.updates` before checking entitlements.**
         A transaction that arrives during startup is otherwise dropped.
      2. **Read `Transaction.currentEntitlements` on every launch**, not just
         after a purchase — the purchase may have happened on another device.
      3. **Ignore unverified results.** `VerificationResult` is the signature
         check; acting on `.unverified` defeats it.
      4. **`.pending` is not a failure.** Under Ask to Buy or Family Sharing
         the purchase completes later through `Transaction.updates`. Telling
         the user "purchase failed" here is simply wrong.

      Always `await transaction.finish()` once you have granted the
      entitlement, and offer a **Restore Purchases** button that calls
      `AppStore.sync()` — App Review requires it for non-consumables.
    assets: [assets/StoreService.swift]
    sources: [storekit_transaction, appstore_sync, review_guidelines]
    verify: A fresh install of a purchased account unlocks after launch without any user action.

  - id: trial_clock
    title: Compute the trial with a high-water mark
    detail: |
      The naive version — `start + 7 days < now` — is defeated by setting the
      clock back. Keep the **highest time ever observed** alongside the start
      date and compute against `max(now, highWaterMark)`:

      ```swift
      func effectiveDate(_ now: Date) -> Date { max(now, highWaterMark) }
      func remaining(at now: Date) -> TimeInterval {
          max(0, end.timeIntervalSince(effectiveDate(now)))
      }
      ```

      Deliberately *not* punished: a user whose clock is wrong for innocent
      reasons loses nothing, they just gain no extra days. Rounding matters
      too — round remaining days **up**, so the last day reads "1 day left"
      rather than "0 days left".

      Keep this logic free of StoreKit so it is testable without a store
      environment.
    assets: [assets/TrialState.swift]
    verify: A unit test that moves the clock backwards does not extend the trial.

  - id: trial_storage
    title: Store the start date where deleting the app cannot reach
    detail: |
      This is the actual problem of a self-built trial. No single store has all
      the properties you need, so use several and merge them — **the earliest
      known start wins**:

      | Store | Survives | Fails at |
      |---|---|---|
      | `UserDefaults` | device backups, migration to a new device | app deletion |
      | Keychain | app deletion on the same device | fresh device |
      | `NSUbiquitousKeyValueStore` (iCloud) | every device on one Apple ID | no iCloud account |

      Two devices without the iCloud store means seven days *each*; app
      deletion without the Keychain means seven days *again*. Write to all of
      them, read the union, and reconcile: if one store knows the start and
      another does not, backfill it — otherwise the next device starts fresh.

      Put the store behind a protocol. `NSUbiquitousKeyValueStore` cannot be
      faked otherwise, and this is exactly the code that decides between "seven
      days total" and "seven days per device".
    verify: Deleting and reinstalling the app on the same device does not reset the remaining days.

  - id: gate_access
    title: Derive access, and degrade instead of locking out
    detail: |
      Resolve access from two inputs and nothing else:

      ```swift
      static func resolve(isPurchased: Bool, trial: TrialState?, now: Date) -> Access {
          if isPurchased { return .purchased }        // a purchase always wins
          if let trial, trial.isActive(at: now) { return .trial(until: trial.end) }
          return .expired
      }
      ```

      A purchase must beat an expired trial, or a paying user lands in the
      locked state — an embarrassing bug that only appears after seven days.

      After expiry, prefer **read-only over unusable**: the user's data belongs
      to the user. Block creating new content, keep viewing, renaming, exporting
      and deleting. It reads as fair rather than hostile, and it avoids the
      review question of whether the app is a demo.
    verify: With an expired trial the app still opens existing content and only blocks creation.

  - id: grandfathering
    title: If the app used to be paid, keep those buyers unlocked
    detail: |
      Switching a paid app to "free plus IAP" must not charge existing buyers
      again. `AppTransaction.originalAppVersion` tells you which version they
      originally got — compare it against the last paid build:

      ```swift
      guard case .verified(let appTransaction)? = try? await AppTransaction.shared,
            isGrandfathered(originalAppVersion: appTransaction.originalAppVersion)
      else { return }
      ```

      Two traps: on iOS `originalAppVersion` is the **build number**
      (`CFBundleVersion`), not the marketing version — parse it as such. And in
      sandbox it reports `"1.0"`, which unlocks everything and makes the paywall
      untestable; gate that check behind a launch argument in debug builds.

      Cache the result so a later launch does not depend on the store.
    sources: [app_transaction]
    verify: A sandbox run without the debug flag still shows the paywall.

  - id: test_it
    title: Test against the StoreKit file first, then the sandbox
    uses: "@speccify/storekit-sandbox-testing@^1.0"

sources:
  - id: asc_iap_types
    title: In-app purchase types
    url: https://developer.apple.com/help/app-store-connect/reference/in-app-purchase-types
    retrieved: 2026-08-06
    note: Which product types exist, and which can carry offers.
  - id: storekit_offers
    title: Introductory offers
    url: https://developer.apple.com/documentation/storekit/product/subscriptioninfo/introductoryoffer
    retrieved: 2026-08-06
    note: Free trials are subscription offers — the reason a self-built clock is needed.
  - id: asc_create_iap
    title: Create in-app purchases
    url: https://developer.apple.com/help/app-store-connect/manage-consumable-and-non-consumable-in-app-purchases/create-consumable-or-non-consumable-in-app-purchases
    retrieved: 2026-08-06
    note: Product ID rules and the permanence of the identifier.
  - id: universal_purchase
    title: Universal purchase
    url: https://developer.apple.com/help/app-store-connect/reference/universal-purchase
    retrieved: 2026-08-06
  - id: xcode_testing
    title: Setting up StoreKit testing in Xcode
    url: https://developer.apple.com/documentation/xcode/setting-up-storekit-testing-in-xcode
    retrieved: 2026-08-06
  - id: storekit_transaction
    title: StoreKit Transaction
    url: https://developer.apple.com/documentation/storekit/transaction
    retrieved: 2026-08-06
    note: currentEntitlements, updates, finish() and VerificationResult.
  - id: appstore_sync
    title: AppStore.sync()
    url: https://developer.apple.com/documentation/storekit/appstore/sync()
    retrieved: 2026-08-06
    note: What "Restore Purchases" has to call.
  - id: review_guidelines
    title: App Review Guidelines — In-App Purchase
    url: https://developer.apple.com/app-store/review/guidelines/
    retrieved: 2026-08-06
    note: Section 3.1.1 — restoring purchases must be possible.
  - id: app_transaction
    title: AppTransaction.originalAppVersion
    url: https://developer.apple.com/documentation/storekit/apptransaction/originalappversion
    retrieved: 2026-08-06
    note: Build number on iOS, marketing version on macOS — and "1.0" in sandbox.

pitfalls:
  - Free trials are subscription-only. A non-consumable cannot have one — if you want "try then own", you build the clock.
  - Product IDs allow no hyphens, so they often cannot mirror a hyphenated bundle ID. They are also permanent.
  - Storing the trial start only in UserDefaults means deleting the app resets it; only in the Keychain means a new device starts over; only in iCloud means no trial without an iCloud account.
  - Checking entitlements before subscribing to `Transaction.updates` drops purchases that land during startup.
  - Treating `.pending` as a failure tells Ask-to-Buy users their purchase broke when it is merely waiting.
  - Letting an expired trial outrank a purchase locks out paying users — and only shows up a week after release.
  - '`AppTransaction.originalAppVersion` reports "1.0" in sandbox, which silently unlocks everything unless the check is gated in debug builds.'
  - Products load as an empty list — with no error — while the paid applications agreement is unsigned.

acceptance:
  - given: a fresh install with no purchase
    when: the app launches for the first time
    then: it reports seven days remaining and allows full use
  - given: an expired trial and no purchase
    when: the app launches
    then: existing content stays usable and only creation is blocked
  - given: a purchase made on another device with the same Apple ID
    when: the app launches
    then: it unlocks without any user action
  - given: an expired trial on a device whose clock is set back one year
    when: the app launches
    then: the trial stays expired
